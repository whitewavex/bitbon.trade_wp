$(document).ready(function() {
    
    var nav =  priorityNav.init ({
        breakPoint: 0,
        throttleDelay: '50',
        navDropdownClassName: 'navigation__dropdown',
        navDropdownToggleClassName: 'navigation__button',
        navDropdownLabel: 'Ещё &nbsp <div class="navigation__img"><div>'
    });
    
});